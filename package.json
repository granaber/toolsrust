{
  "name": "toolrust",
  "version": "0.1.6",
  "description": "Tools for NodeJS",
  "main": "index.node",
  "scripts": {
    "build-cargo": "cargo-cp-artifact -nc index.node -- cargo build --message-format=json-render-diagnostics",
    "build-debug": "npm run build-cargo --",
    "build-release": "npm run build-cargo -- --release",
    "install-cargo": "npm run build-release",
    "test": "cargo test"
  },
  "author": "zellathor Angel Granadillo ",
  "license": "ISC",
  "devDependencies": {
    "cargo-cp-artifact": "^0.1"
  }
}
